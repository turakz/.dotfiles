## nvim extension for claude code that can be installed via packer or lazy
# this can actually achieve the pseudo dream of PITM refactoring tool
=> we could even hardcode this as a principle/header to piped code snippets

given your lua expertise, you might consider writing a thin wrapper plugin that:

sends selected code or entire buffers to claude
captures responses in a split window
potentially uses telescope for searching through conversation history
